# 🎰 دليل البوت الشامل - بوت عجلة الحظ Discord

---

## 📋 جدول المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [متطلبات التشغيل](#متطلبات-التشغيل)
3. [خطوات التثبيت](#خطوات-التثبيت)
4. [إعداد البوت](#إعداد-البوت)
5. [شرح الأوامر بالتفصيل](#شرح-الأوامر-بالتفصيل)
6. [نظام الصلاحيات](#نظام-الصلاحيات)
7. [نظام النقاط](#نظام-النقاط)
8. [نظام العجلات](#نظام-العجلات)
9. [رفع الصور](#رفع-الصور)
10. [أمثلة عملية](#أمثلة-عملية)
11. [حل المشاكل](#حل-المشاكل)
12. [نصائح وإرشادات](#نصائح-وإرشادات)

---

## 🎯 نظرة عامة

بوت Discord متخصص في نظام عجلات الحظ مع:
- ✅ عجلة متحركة تدور لمدة 3.5 ثانية
- ✅ نظام نقاط كامل
- ✅ صلاحيات محددة بالـ Discord ID
- ✅ رفع الصور عبر روابط (بدون ملفات محلية)
- ✅ عجلات متعددة بأيتمات ونسب مختلفة
- ✅ تأثيرات بصرية جميلة

---

## 💻 متطلبات التشغيل

### البرامج المطلوبة:
1. **Node.js** (الإصدار 16.9.0 أو أحدث)
   - تحميل من: https://nodejs.org
   - تأكد من التثبيت: `node --version`

2. **محرر نصوص** (اختياري)
   - VS Code, Notepad++, أو أي محرر آخر

### المتطلبات الأخرى:
- حساب Discord
- صلاحية إنشاء تطبيقات في Discord Developer Portal
- صلاحية إضافة بوت إلى سيرفر Discord

---

## 🚀 خطوات التثبيت

### 1. تثبيت Node.js
1. اذهب إلى https://nodejs.org
2. حمّل النسخة LTS (الموصى بها)
3. ثبّت البرنامج
4. افتح Terminal/CMD واكتب: `node --version`
5. يجب أن تظهر رقم الإصدار

### 2. فك ضغط ملفات البوت
1. فك ضغط ملف `discord-wheel-bot.zip`
2. ضع المجلد في مكان سهل الوصول (مثلاً: Desktop)

### 3. تثبيت المكتبات
1. افتح Terminal/CMD في مجلد البوت
2. اكتب: `npm install`
3. انتظر حتى ينتهي التحميل

---

## ⚙️ إعداد البوت

### الخطوة 1: إنشاء البوت في Discord

1. **اذهب إلى Discord Developer Portal:**
   - https://discord.com/developers/applications

2. **إنشاء Application جديد:**
   - اضغط "New Application"
   - اختر اسم للبوت (مثلاً: "Wheel Bot")
   - اضغط "Create"

3. **إنشاء البوت:**
   - اذهب إلى تبويب "Bot" من القائمة الجانبية
   - اضغط "Add Bot"
   - اضغط "Yes, do it!"

4. **نسخ Token:**
   - في صفحة Bot، اضغط "Reset Token"
   - انسخ الـ Token (⚠️ لا تشاركه مع أحد!)
   - احتفظ به في مكان آمن

5. **تفعيل Intents:**
   - في صفحة Bot، انزل للأسفل
   - فعّل هذه الخيارات:
     * ✅ Presence Intent
     * ✅ Server Members Intent
     * ✅ Message Content Intent
   - اضغط "Save Changes"

### الخطوة 2: دعوة البوت للسيرفر

1. **إنشاء رابط الدعوة:**
   - اذهب إلى تبويب "OAuth2" > "URL Generator"
   - في Scopes، اختر:
     * ✅ bot
     * ✅ applications.commands
   - في Bot Permissions، اختر:
     * ✅ Administrator (أو حدد الصلاحيات المطلوبة)

2. **دعوة البوت:**
   - انسخ الرابط المُنشأ
   - افتحه في المتصفح
   - اختر السيرفر المطلوب
   - اضغط "Authorize"

### الخطوة 3: إعداد ملف config.json

افتح ملف `config.json` وعدّل المعلومات:

```json
{
  "token": "ضع_توكن_البوت_هنا",
  "clientId": "ضع_application_id_هنا",
  "guildId": "ضع_server_id_هنا"
}
```

**للحصول على المعلومات:**

- **Token:** من صفحة Bot في Developer Portal
- **Client ID:** من صفحة General Information (Application ID)
- **Guild ID (Server ID):**
  1. في Discord، فعّل Developer Mode من Settings > Advanced
  2. اضغط بزر الفأرة الأيمن على اسم السيرفر
  3. اختر "Copy ID"

**مثال:**
```json
{
  "token": "MTIzNDU2Nzg5MDEyMzQ1Njc4.GaBcDe.FgHiJkLmNoPqRsTuVwXyZ",
  "clientId": "1234567890123456789",
  "guildId": "9876543210987654321"
}
```

### الخطوة 4: إعداد المستخدمين المصرح لهم

افتح ملف `data/authorized.json`:

```json
{
  "authorizedUsers": [
    "ضع_user_id_الأول",
    "ضع_user_id_الثاني"
  ]
}
```

**للحصول على User ID:**
1. في Discord، تأكد من تفعيل Developer Mode
2. اضغط بزر الفأرة الأيمن على اسم المستخدم
3. اختر "Copy ID"

**مثال:**
```json
{
  "authorizedUsers": [
    "123456789012345678",
    "987654321098765432",
    "555666777888999000"
  ]
}
```

⚠️ **مهم:** فقط المستخدمين في هذه القائمة يمكنهم استخدام الأوامر!

### الخطوة 5: تسجيل الأوامر

في Terminal/CMD:
```bash
node deploy-commands.js
```

يجب أن تظهر رسالة: `تم تسجيل الأوامر بنجاح!`

### الخطوة 6: تشغيل البوت

```bash
node bot.js
```

يجب أن تظهر: `تم تسجيل الدخول كـ [اسم البوت]`

🎉 **مبروك! البوت يعمل الآن!**

---

## 📜 شرح الأوامر بالتفصيل

### 🔧 أوامر إدارة العجلات

#### 1. `/انشاء-عجلة`
**الوصف:** إنشاء عجلة حظ جديدة

**المعاملات:**
- `اسم` (إجباري): اسم العجلة (فريد، لا يمكن تكراره)
- `تكلفة` (إجباري): عدد النقاط المطلوبة للف
- `صورة` (اختياري): رابط صورة العجلة

**أمثلة:**
```
/انشاء-عجلة اسم:الذهبية تكلفة:50
/انشاء-عجلة اسم:الماسية تكلفة:100 صورة:https://i.imgur.com/wheel.png
```

**ملاحظات:**
- اسم العجلة يجب أن يكون فريد
- التكلفة يجب أن تكون رقم موجب
- الصورة اختيارية تماماً

---

#### 2. `/اضافة-ايتم`
**الوصف:** إضافة أيتم (جائزة) لعجلة موجودة

**المعاملات:**
- `عجلة` (إجباري): اسم العجلة
- `اسم` (إجباري): اسم الأيتم
- `كمية` (إجباري): كمية الأيتم
- `نسبة` (إجباري): نسبة ظهور الأيتم (0-100)
- `صورة` (اختياري): رابط صورة الأيتم

**أمثلة:**
```
/اضافة-ايتم عجلة:الذهبية اسم:دنجن كمية:50 نسبة:60
/اضافة-ايتم عجلة:الذهبية اسم:سيف_نادر كمية:1 نسبة:5 صورة:https://i.imgur.com/sword.png
```

**ملاحظات:**
- النسبة بين 0-100
- مجموع النسب لا يجب أن يساوي 100 بالضبط
- يمكن إضافة عدد غير محدود من الأيتمات

**كيفية عمل النسب:**
إذا كان عندك:
- دنجن: نسبة 60%
- أحجار: نسبة 30%
- سيف نادر: نسبة 10%

المجموع = 100، لكن لو كان:
- دنجن: نسبة 80
- أحجار: نسبة 15
- سيف: نسبة 5

المجموع = 100 أيضاً، البوت يحسب النسب نسبياً

---

#### 3. `/حذف-عجلة`
**الوصف:** حذف عجلة بالكامل مع جميع أيتماتها

**المعاملات:**
- `اسم` (إجباري): اسم العجلة المراد حذفها

**مثال:**
```
/حذف-عجلة اسم:القديمة
```

⚠️ **تحذير:** هذا الأمر لا يمكن التراجع عنه!

---

#### 4. `/حذف-ايتم`
**الوصف:** حذف أيتم معين من عجلة

**المعاملات:**
- `عجلة` (إجباري): اسم العجلة
- `ايتم` (إجباري): اسم الأيتم المراد حذفه

**مثال:**
```
/حذف-ايتم عجلة:الذهبية ايتم:سيف_قديم
```

---

#### 5. `/عرض-عجلة`
**الوصف:** عرض تفاصيل عجلة معينة وجميع أيتماتها

**المعاملات:**
- `اسم` (إجباري): اسم العجلة

**مثال:**
```
/عرض-عجلة اسم:الذهبية
```

**يعرض:**
- تكلفة اللف
- عدد الأيتمات
- قائمة بجميع الأيتمات مع نسبها
- صورة العجلة (إن وجدت)

---

#### 6. `/العجلات`
**الوصف:** عرض قائمة بجميع العجلات المتاحة

**لا معاملات**

**مثال:**
```
/العجلات
```

---

### 💰 أوامر إدارة النقاط

#### 7. `/اضافة-نقاط`
**الوصف:** إضافة نقاط لمستخدم

**المعاملات:**
- `مستخدم` (إجباري): المستخدم (@منشن)
- `عدد` (إجباري): عدد النقاط المراد إضافتها

**مثال:**
```
/اضافة-نقاط مستخدم:@Player1 عدد:100
```

**ملاحظات:**
- النقاط تُضاف إلى رصيد المستخدم الحالي
- يمكن إضافة أي عدد موجب

---

#### 8. `/حذف-نقاط`
**الوصف:** حذف نقاط من مستخدم

**المعاملات:**
- `مستخدم` (إجباري): المستخدم (@منشن)
- `عدد` (إجباري): عدد النقاط المراد حذفها

**مثال:**
```
/حذف-نقاط مستخدم:@Player1 عدد:50
```

**ملاحظات:**
- إذا كانت النقاط المحذوفة أكبر من الرصيد، يصبح الرصيد 0
- لا يمكن أن تصبح النقاط سالبة

---

#### 9. `/تعيين-نقاط`
**الوصف:** تعيين عدد نقاط محدد لمستخدم (يستبدل الرصيد الحالي)

**المعاملات:**
- `مستخدم` (إجباري): المستخدم (@منشن)
- `عدد` (إجباري): عدد النقاط الجديد

**مثال:**
```
/تعيين-نقاط مستخدم:@Player1 عدد:500
```

**ملاحظات:**
- يحذف الرصيد القديم ويضع الرصيد الجديد
- مفيد لإعادة تعيين رصيد معين

---

#### 10. `/نقاطي`
**الوصف:** عرض نقاطك الحالية

**لا معاملات**

**مثال:**
```
/نقاطي
```

**ملاحظات:**
- الرسالة تظهر لك فقط (ephemeral)
- إذا لم تلف أي عجلة من قبل، سيكون رصيدك 0

---

#### 11. `/سجل-النقاط`
**الوصف:** عرض قائمة بأعلى 10 مستخدمين من حيث النقاط

**لا معاملات**

**مثال:**
```
/سجل-النقاط
```

**يعرض:**
- ترتيب المستخدمين
- المنشن
- عدد النقاط

---

### 🎰 أوامر اللعب

#### 12. `/لف`
**الوصف:** لف عجلة الحظ

**المعاملات:**
- `عجلة` (إجباري): اسم العجلة
- `مستخدم` (إجباري): المستخدم الذي سيلف (@منشن)

**مثال:**
```
/لف عجلة:الذهبية مستخدم:@Player1
```

**كيف يعمل:**
1. ✅ يتحقق من نقاط المستخدم
2. ✅ يخصم تكلفة اللف
3. ✅ تبدأ العجلة بالدوران (3.5 ثانية)
4. ✅ تعرض أيتمات عشوائية أثناء الدوران
5. ✅ تظهر النتيجة النهائية مع صورة الجائزة

**ملاحظات:**
- إذا لم يكن لدى المستخدم نقاط كافية، تظهر رسالة خطأ
- النقاط تُخصم فوراً قبل اللف
- النتيجة عشوائية حسب النسب المحددة

---

### 👥 أوامر إدارة الصلاحيات

#### 13. `/اضافة-مصرح`
**الوصف:** إضافة مستخدم للقائمة المصرح لها باستخدام البوت

**المعاملات:**
- `ايدي` (إجباري): Discord User ID

**مثال:**
```
/اضافة-مصرح ايدي:123456789012345678
```

**ملاحظات:**
- يجب أن تكون أنت في القائمة المصرح لها لاستخدام هذا الأمر
- User ID يمكن الحصول عليه عبر "Copy ID"

---

#### 14. `/حذف-مصرح`
**الوصف:** إزالة مستخدم من القائمة المصرح لها

**المعاملات:**
- `ايدي` (إجباري): Discord User ID

**مثال:**
```
/حذف-مصرح ايدي:123456789012345678
```

⚠️ **تحذير:** لا تحذف User ID الخاص بك وإلا لن تتمكن من استخدام البوت!

---

## 🔐 نظام الصلاحيات

### كيف يعمل:
1. البوت يقرأ ملف `data/authorized.json`
2. كل أمر يتحقق من User ID المستخدم
3. إذا كان موجود في القائمة → يُنفذ الأمر
4. إذا لم يكن موجود → لا يرد البوت (silent ignore)

### إضافة مستخدمين:
**الطريقة الأولى:** تعديل الملف يدوياً
```json
{
  "authorizedUsers": [
    "123456789012345678",
    "987654321098765432"
  ]
}
```

**الطريقة الثانية:** استخدام الأمر
```
/اضافة-مصرح ايدي:555666777888999000
```

### الحصول على User ID:
1. في Discord، اذهب إلى Settings > Advanced
2. فعّل "Developer Mode"
3. اضغط بزر الفأرة الأيمن على اسم أي مستخدم
4. اختر "Copy ID"

---

## 💎 نظام النقاط

### كيف يعمل:
- كل مستخدم له رصيد نقاط مستقل
- النقاط محفوظة في `data/users.json`
- عند لف العجلة، تُخصم التكلفة تلقائياً
- لا يمكن اللف إذا كانت النقاط غير كافية

### إعطاء نقاط للاعبين:
```
/اضافة-نقاط مستخدم:@Player1 عدد:1000
/اضافة-نقاط مستخدم:@Player2 عدد:500
```

### تعديل النقاط:
```
/حذف-نقاط مستخدم:@Player1 عدد:100
/تعيين-نقاط مستخدم:@Player1 عدد:2000
```

### عرض النقاط:
```
/نقاطي              # نقاطك الشخصية
/سجل-النقاط         # قائمة بالجميع
```

---

## 🎡 نظام العجلات

### إنشاء عجلة:
```
/انشاء-عجلة اسم:المبتدئين تكلفة:10
```

### إضافة أيتمات:
```
/اضافة-ايتم عجلة:المبتدئين اسم:ذهب كمية:100 نسبة:70
/اضافة-ايتم عجلة:المبتدئين اسم:سيف كمية:1 نسبة:20
/اضافة-ايتم عجلة:المبتدئين اسم:دنجن كمية:10 نسبة:10
```

### فهم النسب:
النسب في العجلة نسبية، يعني:
- **70% ذهب** = 7 من كل 10 لفات تقريباً
- **20% سيف** = 2 من كل 10 لفات
- **10% دنجن** = 1 من كل 10 لفات

**مثال:** إذا حطيت:
- أيتم A: نسبة 80
- أيتم B: نسبة 20

النتيجة: أيتم A يطلع 4 أضعاف أيتم B

---

## 🖼️ رفع الصور

### الطريقة الأولى: Discord CDN
1. ارفع الصورة في أي قناة Discord
2. اضغط بزر الفأرة الأيمن على الصورة
3. اختر "Copy Link" أو "نسخ الرابط"
4. الصق الرابط في الأمر

**مثال:**
```
/انشاء-عجلة اسم:الخاصة تكلفة:50 صورة:https://cdn.discordapp.com/attachments/123/456/image.png
```

### الطريقة الثانية: Imgur
1. اذهب إلى https://imgur.com
2. اضغط "New post"
3. ارفع الصورة
4. انسخ "Direct Link"
5. الصق في الأمر

**مثال:**
```
/اضافة-ايتم عجلة:الذهبية اسم:تاج كمية:1 نسبة:5 صورة:https://i.imgur.com/abc123.png
```

### نصائح:
- ✅ استخدم صور بجودة جيدة (PNG أو JPG)
- ✅ الحجم الأمثل: 512x512 أو 1024x1024
- ✅ تأكد من أن الرابط ينتهي بـ .png أو .jpg أو .gif
- ❌ لا تستخدم روابط مواقع تحتاج تسجيل دخول

---

## 📖 أمثلة عملية

### مثال 1: إنشاء عجلة بسيطة

**الخطوة 1:** إنشاء العجلة
```
/انشاء-عجلة اسم:المبتدئين تكلفة:10
```

**الخطوة 2:** إضافة أيتمات
```
/اضافة-ايتم عجلة:المبتدئين اسم:عملات كمية:50 نسبة:60
/اضافة-ايتم عجلة:المبتدئين اسم:خبرة كمية:100 نسبة:30
/اضافة-ايتم عجلة:المبتدئين اسم:جوهرة كمية:1 نسبة:10
```

**الخطوة 3:** إعطاء نقاط للاعب
```
/اضافة-نقاط مستخدم:@Player1 عدد:100
```

**الخطوة 4:** اللف!
```
/لف عجلة:المبتدئين مستخدم:@Player1
```

---

### مثال 2: عجلة متقدمة مع صور

**الخطوة 1:** رفع صورة العجلة
1. ارفع صورة عجلة في Discord
2. انسخ الرابط

**الخطوة 2:** إنشاء العجلة مع صورة
```
/انشاء-عجلة اسم:الأسطورية تكلفة:100 صورة:https://cdn.discordapp.com/attachments/.../wheel.png
```

**الخطوة 3:** إضافة أيتمات مع صور
```
/اضافة-ايتم عجلة:الأسطورية اسم:سيف_التنين كمية:1 نسبة:5 صورة:https://i.imgur.com/dragon_sword.png
/اضافة-ايتم عجلة:الأسطورية اسم:درع_الأساطير كمية:1 نسبة:5 صورة:https://i.imgur.com/legend_armor.png
/اضافة-ايتم عجلة:الأسطورية اسم:جواهر كمية:500 نسبة:40 صورة:https://i.imgur.com/gems.png
/اضافة-ايتم عجلة:الأسطورية اسم:ذهب كمية:1000 نسبة:50 صورة:https://i.imgur.com/gold.png
```

**الخطوة 4:** إعطاء نقاط
```
/تعيين-نقاط مستخدم:@VIPPlayer عدد:1000
```

**الخطوة 5:** اللف
```
/لف عجلة:الأسطورية مستخدم:@VIPPlayer
```

---

### مثال 3: سيناريو كامل - Event خاص

**السيناريو:** عندك Event خاص وتريد عمل عجلة للجوائز

**الخطوة 1:** تجهيز البوت
```bash
# في Terminal/CMD
node bot.js
```

**الخطوة 2:** إنشاء عجلة Event
```
/انشاء-عجلة اسم:عيد_الفطر تكلفة:0 صورة:https://i.imgur.com/eid_wheel.png
```
ملاحظة: التكلفة 0 معناها مجاني!

**الخطوة 3:** إضافة جوائز
```
/اضافة-ايتم عجلة:عيد_الفطر اسم:نيترو_شهر كمية:1 نسبة:2
/اضافة-ايتم عجلة:عيد_الفطر اسم:رول_VIP كمية:1 نسبة:5
/اضافة-ايتم عجلة:عيد_الفطر اسم:خصم_50% كمية:1 نسبة:10
/اضافة-ايتم عجلة:عيد_الفطر اسم:شكراً كمية:1 نسبة:83
```

**الخطوة 4:** إعطاء الجميع فرصة واحدة
```
/تعيين-نقاط مستخدم:@User1 عدد:0
/تعيين-نقاط مستخدم:@User2 عدد:0
/تعيين-نقاط مستخدم:@User3 عدد:0
```
الآن صفّر رصيد الجميع، ثم أعطهم فرصة واحدة:
```
/اضافة-نقاط مستخدم:@User1 عدد:1
/اضافة-نقاط مستخدم:@User2 عدد:1
/اضافة-نقاط مستخدم:@User3 عدد:1
```

⚠️ **ملاحظة:** لو عندك أعضاء كثيرين، الأفضل عمل نظام تفاعلي منفصل

**الخطوة 5:** اللف في Event
```
/لف عجلة:عيد_الفطر مستخدم:@User1
```

---

### مثال 4: نظام مكافآت يومي

**الفكرة:** كل يوم تعطي اللاعبين نقاط مجانية

**عمل عجلة يومية:**
```
/انشاء-عجلة اسم:اليومية تكلفة:1
/اضافة-ايتم عجلة:اليومية اسم:عملات_قليلة كمية:10 نسبة:70
/اضافة-ايتم عجلة:اليومية اسم:عملات_متوسطة كمية:50 نسبة:25
/اضافة-ايتم عجلة:اليومية اسم:عملات_كثيرة كمية:200 نسبة:5
```

**إعطاء اللاعبين نقطة واحدة يومياً:**
```
# صباح كل يوم
/اضافة-نقاط مستخدم:@Player1 عدد:1
/اضافة-نقاط مستخدم:@Player2 عدد:1
```

**اللف:**
```
/لف عجلة:اليومية مستخدم:@Player1
```

---

## 🔧 حل المشاكل

### المشكلة 1: البوت لا يظهر أونلاين

**الحلول:**
1. ✅ تأكد من تشغيل `node bot.js`
2. ✅ تحقق من صحة Token في `config.json`
3. ✅ تأكد من تفعيل جميع Intents في Developer Portal
4. ✅ تحقق من اتصالك بالإنترنت

---

### المشكلة 2: الأوامر لا تظهر

**الحلول:**
1. ✅ شغّل `node deploy-commands.js`
2. ✅ تحقق من صحة `clientId` و `guildId` في config.json
3. ✅ انتظر 5-10 دقائق (أحياناً Discord يتأخر)
4. ✅ جرّب إعادة تشغيل Discord

**لحذف الأوامر القديمة:**
```bash
node delete-commands.js
node deploy-commands.js
```

---

### المشكلة 3: البوت لا يرد على أوامري

**الحلول:**
1. ✅ تأكد من أن User ID الخاص بك في `data/authorized.json`
2. ✅ تحقق من تهجئة User ID بشكل صحيح
3. ✅ استخدم "Copy ID" لنسخ الـ ID بدقة

**للتحقق:**
افتح `data/authorized.json` ويجب أن يكون:
```json
{
  "authorizedUsers": [
    "YOUR_USER_ID_HERE"
  ]
}
```

---

### المشكلة 4: Error عند تثبيت المكتبات

**إذا ظهرت رسالة `npm install` error:**

1. احذف مجلد `node_modules`
2. احذف ملف `package-lock.json`
3. شغّل: `npm install` مرة أخرى

**إذا ظهرت vulnerabilities:**
- تجاهلها، البوت سيعمل عادي
- أو شغّل: `npm audit fix`

---

### المشكلة 5: العجلة لا تلف (البوت يرد لكن لا يلف)

**أسباب محتملة:**
1. ❌ العجلة فارغة (لا توجد أيتمات)
   - **الحل:** أضف أيتمات بـ `/اضافة-ايتم`

2. ❌ المستخدم ليس لديه نقاط كافية
   - **الحل:** أعطه نقاط بـ `/اضافة-نقاط`

3. ❌ اسم العجلة خاطئ
   - **الحل:** تحقق من الاسم بـ `/العجلات`

---

### المشكلة 6: الصور لا تظهر

**الحلول:**
1. ✅ تأكد من أن الرابط صحيح وينتهي بـ .png أو .jpg
2. ✅ افتح الرابط في المتصفح للتأكد
3. ✅ استخدم روابط مباشرة (Direct Links)
4. ✅ تجنب روابط Google Drive (لا تعمل مباشرة)

**روابط موصى بها:**
- ✅ Discord CDN: `https://cdn.discordapp.com/...`
- ✅ Imgur: `https://i.imgur.com/...`
- ❌ Google Drive: لا يعمل مباشرة

---

### المشكلة 7: خطأ في البيانات (Data corruption)

إذا تعطل ملف JSON:

**الحل 1:** إصلاح يدوي
1. افتح الملف المعطل (users.json أو wheels.json)
2. تأكد من صحة بنية JSON
3. استخدم أداة مثل https://jsonlint.com للتحقق

**الحل 2:** إعادة تعيين
احذف الملف وأنشئه من جديد:

`data/users.json`:
```json
{
  "users": {}
}
```

`data/wheels.json`:
```json
{
  "wheels": {}
}
```

⚠️ **تحذير:** ستخسر جميع البيانات!

---

## 💡 نصائح وإرشادات

### نصائح للاستخدام الأمثل:

1. **نسخ احتياطية:**
   - انسخ مجلد `data` كل فترة
   - احتفظ بنسخة من `config.json`

2. **تنظيم العجلات:**
   - استخدم أسماء واضحة (مثلاً: "المبتدئين", "المتقدمين", "الأسطورية")
   - لا تستخدم رموز أو أحرف خاصة

3. **توزيع النسب:**
   - اجعل الجوائز النادرة نسبتها قليلة (1-5%)
   - اجعل الجوائز العادية نسبتها عالية (60-80%)
   - الجوائز المتوسطة بين 10-30%

4. **الصور:**
   - استخدم صور عالية الجودة
   - حجم 512x512 أو 1024x1024 مثالي
   - احفظ روابط الصور في ملف نصي

5. **الأمان:**
   - ⚠️ **لا تشارك Token البوت مع أحد!**
   - لا ترفع `config.json` على GitHub أو أي موقع
   - احتفظ بقائمة المصرح لهم محدثة

### أفكار إبداعية:

1. **Event Wheels:**
   - عجلة رمضان
   - عجلة العيد
   - عجلة الذكرى السنوية

2. **نظام Ranks:**
   - عجلة مبتدئين (تكلفة 10)
   - عجلة متقدمين (تكلفة 50)
   - عجلة VIP (تكلفة 100)

3. **جوائز متنوعة:**
   - Discord Nitro
   - رولات خاصة
   - عملات السيرفر
   - دخول مسابقات

4. **التحديات:**
   - "لف 10 مرات واحصل على بونص"
   - "أول من يحصل على الجائزة النادرة يفوز"

---

## 📊 معلومات تقنية إضافية

### بنية الملفات:

```
discord-wheel-bot/
├── bot.js                  # الكود الرئيسي
├── deploy-commands.js      # تسجيل الأوامر
├── delete-commands.js      # حذف الأوامر
├── config.json            # إعدادات البوت
├── package.json           # معلومات المشروع
├── data/
│   ├── authorized.json    # قائمة المصرح لهم
│   ├── users.json         # نقاط المستخدمين
│   └── wheels.json        # بيانات العجلات
└── images/               # مجلد اختياري للصور المحلية
```

### كيفية عمل النظام:

1. **التحقق من الصلاحيات:**
   - عند كل أمر، يتحقق البوت من User ID
   - إذا موجود في authorized.json → ينفذ
   - إذا غير موجود → يتجاهل

2. **نظام النقاط:**
   - محفوظ في `users.json`
   - كل User ID له رصيد مستقل
   - التحديث فوري

3. **اختيار عشوائي:**
   - يستخدم `weightedRandom` function
   - يحسب النسب نسبياً
   - عشوائية عادلة

4. **العجلة المتحركة:**
   - 3 تحديثات للرسالة
   - كل تحديث بعد 800ms
   - مجموع الوقت: ~3.5 ثانية

---

## 🎓 خلاصة سريعة

### للبدء السريع:
```bash
# 1. تثبيت المكتبات
npm install

# 2. تعديل config.json (Token, IDs)
# 3. تعديل authorized.json (User IDs)

# 4. تسجيل الأوامر
node deploy-commands.js

# 5. تشغيل البوت
node bot.js
```

### أول عجلة:
```
/انشاء-عجلة اسم:الأولى تكلفة:10
/اضافة-ايتم عجلة:الأولى اسم:ذهب كمية:100 نسبة:80
/اضافة-ايتم عجلة:الأولى اسم:جوهرة كمية:1 نسبة:20
/اضافة-نقاط مستخدم:@نفسك عدد:100
/لف عجلة:الأولى مستخدم:@نفسك
```

---

## 📞 الدعم والمساعدة

### إذا واجهت مشكلة:

1. **راجع قسم [حل المشاكل](#حل-المشاكل)**
2. **تأكد من:**
   - ✅ Node.js مُثبت
   - ✅ Token صحيح
   - ✅ IDs صحيحة
   - ✅ Intents مُفعلة
   - ✅ البوت أونلاين

3. **أعد تشغيل البوت:**
   ```bash
   # أغلق البوت (Ctrl+C)
   node bot.js
   ```

### ملاحظات أخيرة:

- 🎯 البوت يعمل 24/7 طالما جهازك شغال
- 💾 جميع البيانات محفوظة محلياً
- 🔒 Token سري للغاية، لا تشاركه أبداً
- 📱 يعمل على Windows, Mac, Linux

---

**تم إنشاء هذا الدليل بواسطة Claude**
**آخر تحديث: 2024**

🎰 **استمتع باستخدام بوت عجلة الحظ!** 🎰
