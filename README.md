# بوت عجلة الحظ Discord

بوت Discord مخصص لنظام عجلات الحظ مع نظام نقاط وصلاحيات.

## المميزات

- ✅ **عجلة ديناميكية مرسومة بالكامل** - ترسم برمجياً باستخدام Canvas
- ✅ **فيزياء واقعية** - محاكاة دوران حقيقي مع تباطؤ تدريجي
- ✅ **تصدير GIF متحرك** - العجلة تدور وتتوقف على الجائزة الفعلية
- ✅ **ألوان تلقائية** لكل قسم في العجلة
- ✅ **أسماء ونسب الجوائز** مكتوبة على العجلة
- ✅ **دقة في التوقف** - المؤشر يتوقف بالضبط على الجائزة المختارة
- ✅ رفع الصور مباشرة عبر روابط
- ✅ نظام نقاط كامل
- ✅ نظام صلاحيات محكم
- ✅ اختيار عشوائي عادل حسب النسب

## التثبيت

### 1. تثبيت Node.js
تأكد من تثبيت Node.js (الإصدار 16.9.0 أو أحدث)

### 2. تثبيت المكتبات
```bash
npm install
```

**⚠️ مهم:** مكتبة `canvas` تحتاج مكتبات إضافية على النظام:

**على Windows:**
- يتم التثبيت تلقائياً مع `npm install`
- إذا فشل، قد تحتاج **Windows Build Tools**:
  ```bash
  npm install --global windows-build-tools
  ```

**على Ubuntu/Debian:**
```bash
sudo apt-get install build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev
```

**على MacOS:**
```bash
brew install pkg-config cairo pango libpng jpeg giflib librsvg
```

### 3. إعداد البوت

#### الحصول على Token:
1. اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications)
2. اضغط "New Application"
3. اختر اسم للبوت
4. اذهب إلى "Bot" في القائمة الجانبية
5. اضغط "Add Bot"
6. اضغط "Reset Token" وانسخ الـ Token

#### تفعيل Intents:
في صفحة Bot، فعّل:
- Presence Intent
- Server Members Intent
- Message Content Intent

#### دعوة البوت للسيرفر:
1. اذهب إلى "OAuth2" > "URL Generator"
2. اختر:
   - Scopes: `bot` و `applications.commands`
   - Bot Permissions: `Administrator` (أو حسب احتياجك)
3. انسخ الرابط وافتحه في المتصفح لدعوة البوت

### 4. تعديل config.json

افتح ملف `config.json` وضع:
```json
{
  "token": "توكن_البوت_هنا",
  "clientId": "client_id_البوت",
  "guildId": "id_السيرفر"
}
```

**للحصول على Client ID:**
- من صفحة Application في Discord Developer Portal، انسخ "Application ID"

**للحصول على Guild ID (Server ID):**
- في Discord، فعّل Developer Mode من Settings > Advanced
- اضغط بزر الفأرة الأيمن على اسم السيرفر واختر "Copy ID"

### 5. تعديل authorized.json

افتح ملف `data/authorized.json` وضع IDs المستخدمين المصرح لهم:
```json
{
  "authorizedUsers": [
    "123456789012345678",
    "987654321098765432"
  ]
}
```

**للحصول على User ID:**
- في Discord، اضغط بزر الفأرة الأيمن على اسم المستخدم واختر "Copy ID"

### 6. إضافة الصور

**الآن لا حاجة لوضع الصور في المجلدات!** 🎉

لإضافة صورة:
1. ارفع الصورة لأي قناة في Discord
2. اضغط بزر الفأرة الأيمن على الصورة > "نسخ الرابط"
3. الصق الرابط في خانة `صورة` عند إنشاء العجلة/الأيتم

**أو استخدم خدمة رفع صور مثل:**
- Imgur (https://imgur.com)
- Discord CDN (ارفع في أي قناة)
- أي رابط مباشر للصورة

**مثال:**
```
/انشاء-عجلة اسم:السوبر تكلفة:100 صورة:https://i.imgur.com/abc123.png
```

### 7. تسجيل الأوامر

```bash
node deploy-commands.js
```

### 8. تشغيل البوت

```bash
npm start
```

أو:
```bash
node bot.js
```

## الأوامر

### أوامر إدارية (للمصرح لهم فقط):

| الأمر | الوصف | مثال |
|-------|-------|------|
| `/انشاء-عجلة` | إنشاء عجلة جديدة | `/انشاء-عجلة اسم:الأساسية صورة:wheel1.png تكلفة:10` |
| `/اضافة-ايتم` | إضافة أيتم لعجلة | `/اضافة-ايتم عجلة:الأساسية اسم:سيف كمية:1 نسبة:20 صورة:sword.png` |
| `/حذف-عجلة` | حذف عجلة | `/حذف-عجلة اسم:الأساسية` |
| `/حذف-ايتم` | حذف أيتم من عجلة | `/حذف-ايتم عجلة:الأساسية ايتم:سيف` |
| `/اضافة-نقاط` | إضافة نقاط لمستخدم | `/اضافة-نقاط مستخدم:@اسم عدد:100` |
| `/حذف-نقاط` | حذف نقاط من مستخدم | `/حذف-نقاط مستخدم:@اسم عدد:50` |
| `/تعيين-نقاط` | تعيين نقاط محددة | `/تعيين-نقاط مستخدم:@اسم عدد:200` |
| `/اضافة-مصرح` | إضافة مستخدم مصرح | `/اضافة-مصرح ايدي:123456789` |
| `/حذف-مصرح` | حذف مستخدم مصرح | `/حذف-مصرح ايدي:123456789` |

### أوامر عامة:

| الأمر | الوصف | مثال |
|-------|-------|------|
| `/لف` | لف عجلة الحظ | `/لف عجلة:الأساسية مستخدم:@اسم` |
| `/نقاطي` | عرض نقاطك | `/نقاطي` |
| `/سجل-النقاط` | عرض نقاط الجميع | `/سجل-النقاط` |
| `/العجلات` | عرض العجلات المتاحة | `/العجلات` |
| `/عرض-عجلة` | عرض تفاصيل عجلة | `/عرض-عجلة اسم:الأساسية` |

## هيكل الملفات

```
discord-wheel-bot/
├── bot.js                  # الملف الرئيسي للبوت
├── wheelRenderer.js        # محرك رسم العجلات بـ Canvas + الفيزياء
├── deploy-commands.js      # تسجيل الأوامر
├── delete-commands.js      # حذف الأوامر القديمة
├── config.json            # إعدادات البوت
├── package.json           # معلومات المشروع
├── GUIDE.md               # دليل شامل بالعربي
├── data/
│   ├── authorized.json    # المستخدمين المصرح لهم
│   ├── users.json         # نقاط المستخدمين
│   └── wheels.json        # بيانات العجلات
└── README.md              # هذا الملف
```

## مثال على الاستخدام

### 1. إنشاء عجلة:
```
/انشاء-عجلة اسم:الذهبية تكلفة:50 صورة:https://i.imgur.com/example.png
```
**ملاحظة:** الصورة اختيارية، يمكن تركها فارغة

### 2. إضافة أيتمات:
```
/اضافة-ايتم عجلة:الذهبية اسم:دنجن كمية:60 نسبة:80 صورة:https://i.imgur.com/dungeon.png
/اضافة-ايتم عجلة:الذهبية اسم:احجار كمية:100 نسبة:15
/اضافة-ايتم عجلة:الذهبية اسم:سلاح_نادر كمية:1 نسبة:5 صورة:https://i.imgur.com/sword.png
```
**ملاحظة:** الصورة اختيارية لكل أيتم

### 3. إضافة نقاط:
```
/اضافة-نقاط مستخدم:@Player1 عدد:200
```

### 4. لف العجلة:
```
/لف عجلة:الذهبية مستخدم:@Player1
```

سيظهر:
1. ✅ رسالة "جارٍ لف العجلة..."
2. ✅ **GIF للعجلة تدور بفيزياء واقعية**
3. ✅ العجلة تتباطأ تدريجياً وتتوقف على الجائزة
4. ✅ النتيجة النهائية مع صورة الأيتم الفائز

**الوقت الكلي:** ~10-12 ثانية (توليد GIF + مشاهدة الدوران)

## ملاحظات

- ✅ فقط المستخدمين المصرح لهم في `authorized.json` يمكنهم استخدام الأوامر
- ✅ المستخدمين غير المصرح لهم لن يروا أي رد من البوت
- ✅ يتم خصم النقاط تلقائياً عند لف العجلة
- ✅ **العجلة مرسومة برمجياً** - لا حاجة لصور جاهزة
- ✅ **فيزياء واقعية** - الدوران يستخدم معادلات فيزيائية
- ✅ **دقة 100%** - المؤشر يتوقف على الجائزة الفعلية المختارة
- ✅ التوليد يستغرق 3-5 ثوانٍ (عملية ثقيلة)
- ✅ مجموع نسب الأيتمات لا يجب أن يساوي 100% - سيتم توزيعها نسبياً

## دعم

إذا واجهت أي مشكلة:
1. تأكد من صحة الـ Token و IDs
2. تأكد من تفعيل جميع الـ Intents
3. تأكد من وجود الصور في المجلدات الصحيحة
4. تحقق من console للأخطاء

## الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه بحرية.
